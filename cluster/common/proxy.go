package common

import (
	"context"

	"github.com/kaidyth/lexa/server/dataset"
)

func HttpProxy(ctx context.Context) ([]dataset.Host, error) {
	return nil, nil
}

func DNSProxy(ctx context.Context) ([]dataset.Host, error) {
	return nil, nil
}
